namespace BedayaGroup.Domain.Enums;

public enum AdvanceStatus
{
    Open = 1,            // مفتوحة
    PartiallySettled = 2,// مسواة جزئياً
    FullySettled = 3,    // مسواة بالكامل
    Closed = 4           // مغلقة
}
